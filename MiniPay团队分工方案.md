# MiniPay 团队分工方案

## 1. 团队组织原则

MiniPay 期末工程由 5 人共同完成。分工不按“前端、后端、文档”简单切分，而是围绕完整系统能力划分责任，确保每位成员都有明确的代码贡献、文档贡献和答辩内容。

本项目必须完成完整主链路：

```text
商户创建订单 -> 用户发起模拟支付 -> 支付状态更新 -> 结果查询与展示
```

同时，本项目明确实现以下增强能力：

1. 登录注册与角色权限控制。
2. RabbitMQ 支付结果事件。
3. 商户通知记录和失败重试。
4. Redis 订单状态缓存、幂等 token 和短期锁。
5. 接口限流、统一日志、链路追踪。
6. GitHub Actions 构建与测试流程。
7. Prometheus + Grafana 指标监控。

## 2. 总体分工表

| 成员 | 角色定位 | 主要负责模块 | 核心交付物 | 答辩重点 |
| --- | --- | --- | --- | --- |
| 成员 A | 组长 / 架构与网关负责人 | 总体架构、API Gateway、接口规范、集成协调 | 架构图、接口规范、Gateway 路由、统一错误返回、演示脚本 | 架构设计、服务拆分、网关作用、系统调用链路 |
| 成员 B | 订单与商户负责人 | `order-service`、`merchant-service`、订单数据库设计、Redis 订单缓存 | 订单创建、订单查询、订单状态机、商户订单视图、订单缓存 | 订单模型、状态流转、缓存一致性、商户查询 |
| 成员 C | 支付与消息负责人 | `payment-service`、支付流水、幂等控制、RabbitMQ 事件 | 模拟支付、支付校验、支付流水、幂等控制、支付结果事件 | 支付校验、重复支付拦截、事件驱动 |
| 成员 D | 认证、通知与运营后台负责人 | `auth-service`、`notify-service`、`admin-service` | 登录注册、Token、角色权限、通知记录、失败重试、后台查询 | 权限设计、通知重试、运营排查能力 |
| 成员 E | 前端、容器化与质量治理负责人 | Vue 前端、Docker Compose、CI、监控、链路追踪、测试 | 支付页、商户后台、运营后台、一键启动、CI、Grafana 面板、测试报告 | 前端演示、容器部署、CI、监控、链路追踪 |

## 3. 成员 A：组长 / 架构与网关负责人

### 3.1 主要职责

成员 A 负责项目整体设计和集成协调，保证各服务接口、数据结构、错误码和演示流程保持一致。

### 3.2 具体任务

1. 维护项目总体架构设计。
2. 绘制系统架构图和核心调用链路图。
3. 设计统一 API 前缀、接口命名规范和统一响应格式。
4. 实现 `gateway-service` 路由转发。
5. 设计统一错误码和异常返回结构。
6. 协调各服务端口、配置项、接口路径和数据字段。
7. 组织主链路联调。
8. 整理最终演示脚本和答辩顺序。

### 3.3 交付物

1. `gateway-service` 代码。
2. 架构说明文档。
3. 接口规范文档。
4. 统一错误码说明。
5. 演示脚本。

### 3.4 答辩内容

1. 为什么采用轻量微服务架构。
2. Gateway 在系统中的作用。
3. 请求从前端到后端服务的完整流转过程。
4. 服务拆分后如何保持接口和数据一致。

## 4. 成员 B：订单与商户负责人

### 4.1 主要职责

成员 B 负责订单和商户相关能力，是系统主链路的起点和结果查询的重要支撑。

### 4.2 具体任务

1. 设计 `merchant` 表和 `pay_order` 表。
2. 实现商户创建订单接口。
3. 生成唯一平台订单号。
4. 实现订单状态机，包括 `PENDING`、`PAYING`、`PAID`、`CLOSED`、`EXPIRED`。
5. 实现订单详情查询接口。
6. 实现商户订单列表查询接口。
7. 接入 Redis 订单状态缓存。
8. 处理支付成功事件，更新订单状态。
9. 保证订单状态更新后缓存能够正确刷新或失效。

### 4.3 交付物

1. `order-service` 代码。
2. `merchant-service` 代码。
3. 订单表和商户表迁移脚本。
4. 订单状态流转说明。
5. 订单缓存设计说明。

### 4.4 答辩内容

1. 订单状态如何流转。
2. 商户订单号和平台订单号的区别。
3. 为什么 PostgreSQL 是权威数据源。
4. Redis 缓存如何避免查询旧状态。

## 5. 成员 C：支付与消息负责人

### 5.1 主要职责

成员 C 负责支付核心逻辑，保证系统能够完成模拟支付，并防止重复支付产生多笔成功流水。

### 5.2 具体任务

1. 设计 `payment_record` 表和 `payment_event` 表。
2. 实现支付页订单支付确认接口。
3. 支付前调用订单服务校验订单是否存在。
4. 校验支付金额是否与订单金额一致。
5. 校验订单状态是否允许支付。
6. 实现幂等控制，支持 `Idempotency-Key`。
7. 使用 Redis 保存幂等 token 或短期锁。
8. 支付成功后写入支付流水。
9. 支付成功后向 RabbitMQ 发布支付结果事件。
10. 处理 RabbitMQ 不可用时的事件补偿或重试记录。

### 5.3 交付物

1. `payment-service` 代码。
2. 支付流水表和支付事件表迁移脚本。
3. 支付接口说明。
4. 幂等设计说明。
5. RabbitMQ 事件格式说明。

### 5.4 答辩内容

1. 支付前需要校验哪些内容。
2. 如何防止重复支付。
3. 为什么支付成功后使用 RabbitMQ 事件。
4. 支付流水和订单状态之间如何保持一致。

## 6. 成员 D：认证、通知与运营后台负责人

### 6.1 主要职责

成员 D 负责系统安全边界、商户通知和运营后台查询能力，使系统不只是单条支付接口，而是具备平台化管理能力。

### 6.2 具体任务

1. 实现 `auth-service`。
2. 实现用户、商户、运营人员登录注册。
3. 实现 Token 签发、刷新和校验。
4. 实现角色权限控制。
5. 实现 `notify-service`。
6. 消费支付成功事件并生成商户通知记录。
7. 记录通知成功、失败、重试次数和下次重试时间。
8. 实现通知失败重试机制。
9. 实现 `admin-service`。
10. 提供运营后台订单、支付流水、事件和通知记录查询接口。

### 6.3 交付物

1. `auth-service` 代码。
2. `notify-service` 代码。
3. `admin-service` 代码。
4. 用户和角色权限说明。
5. 通知记录表迁移脚本。
6. 后台查询接口说明。

### 6.4 答辩内容

1. 不同角色的权限如何区分。
2. Token 在系统中如何校验。
3. 商户通知为什么不能阻塞支付主流程。
4. 通知失败如何重试。
5. 运营后台如何帮助排查异常订单。

## 7. 成员 E：前端、容器化与质量治理负责人

### 7.1 主要职责

成员 E 负责用户可见页面、部署运行和质量治理，是最终演示效果和工程化交付的主要负责人。

### 7.2 具体任务

1. 实现 Vue 3 前端项目。
2. 实现支付页，展示订单信息并发起模拟支付。
3. 实现商户后台，包括创建订单、订单列表和订单详情。
4. 实现运营后台，包括订单查询、流水查询、事件查询和通知查询。
5. 编写前端 Dockerfile。
6. 编写后端服务 Dockerfile 模板。
7. 编写 `docker-compose.yml`。
8. 配置 PostgreSQL、Redis、RabbitMQ、Nginx、Prometheus、Grafana、SkyWalking、Sentinel 等服务。
9. 配置 GitHub Actions 构建与测试流程。
10. 整理接口测试结果、页面截图、CI 截图、监控面板截图。

### 7.3 交付物

1. 前端项目代码。
2. `docker-compose.yml`。
3. 各服务 Dockerfile。
4. `.env.example`。
5. GitHub Actions workflow。
6. 测试报告。
7. Grafana 监控面板截图。
8. 链路追踪截图。

### 7.4 答辩内容

1. 前端如何串起完整主链路。
2. Docker Compose 如何一键启动系统。
3. CI 流程检查了哪些内容。
4. Prometheus + Grafana 展示了哪些指标。
5. 链路追踪如何定位跨服务问题。

## 8. 协作方式

### 8.1 分支管理

建议使用以下分支方式：

```text
main
develop
feature/gateway
feature/order-merchant
feature/payment-mq
feature/auth-notify-admin
feature/frontend-devops
```

协作要求：

1. `main` 分支只保留稳定版本。
2. 日常开发合并到 `develop`。
3. 每位成员从自己的 `feature/*` 分支开发。
4. 合并前必须确认项目能构建或至少不影响主链路。
5. 重大接口变更必须先同步给成员 A。

### 8.2 接口协作规则

1. 所有接口必须使用统一响应格式。
2. 接口路径、请求字段、响应字段变更必须同步更新接口文档。
3. 前后端联调前先准备 mock 数据或接口示例。
4. 支付、订单、通知相关字段必须统一使用相同命名，例如 `orderNo`、`paymentNo`、`merchantNo`。
5. 错误码统一维护，不能各服务随意返回不同格式。

### 8.3 每日联调检查

每天至少检查一次主链路：

1. 能否登录。
2. 能否创建订单。
3. 能否打开支付页。
4. 能否完成模拟支付。
5. 订单状态是否更新为已支付。
6. 商户后台是否能查到订单和流水。
7. 运营后台是否能查到事件和通知记录。
8. Redis、RabbitMQ、Prometheus、Grafana 是否正常运行。

## 9. 阶段计划

| 阶段 | 时间建议 | 主要目标 | 负责人 |
| --- | --- | --- | --- |
| 第一阶段 | 第 1-2 天 | 确认范围、建立项目骨架、确定接口规范 | 成员 A 牵头，全员参与 |
| 第二阶段 | 第 3-5 天 | 完成订单、支付、商户主链路接口 | 成员 B、C |
| 第三阶段 | 第 6-7 天 | 完成认证、通知、运营后台接口 | 成员 D |
| 第四阶段 | 第 8-9 天 | 完成前端页面和前后端联调 | 成员 E 牵头，全员配合 |
| 第五阶段 | 第 10-11 天 | 接入 Redis、RabbitMQ、限流、日志、链路追踪、监控 | 成员 C、D、E |
| 第六阶段 | 第 12 天 | 完成 Docker Compose、CI 和测试报告 | 成员 E |
| 第七阶段 | 第 13-14 天 | 准备演示、文档、答辩分工和最终检查 | 成员 A 牵头，全员参与 |

## 10. 答辩分工

答辩建议按完整系统链路展开：

1. 成员 A 介绍项目背景、系统范围、总体架构和服务拆分。
2. 成员 B 演示商户创建订单、订单查询和订单状态流转。
3. 成员 C 演示模拟支付、支付幂等和 RabbitMQ 支付结果事件。
4. 成员 D 演示登录权限、商户通知记录、失败重试和运营后台查询。
5. 成员 E 演示前端页面、Docker Compose 一键启动、CI、监控面板和链路追踪。

## 11. 最终提交分工

| 提交内容 | 主要负责人 | 配合成员 |
| --- | --- | --- |
| 代码仓库整理 | 成员 A | 全员 |
| README 与启动说明 | 成员 E | 成员 A |
| 需求摘要 | 成员 A | 全员 |
| 架构说明 | 成员 A | 成员 B、C、D |
| 接口说明 | 成员 A | 成员 B、C、D |
| 数据库设计说明 | 成员 B | 成员 C、D |
| 部署说明 | 成员 E | 成员 A |
| 测试报告 | 成员 E | 全员 |
| 演示脚本 | 成员 A | 全员 |
| 分工与贡献说明 | 成员 A | 全员确认 |
| CI、监控、链路追踪截图 | 成员 E | 成员 C、D |

## 12. 个人贡献填写模板

每位成员最终提交时按以下格式填写：

```text
姓名：
负责模块：
完成工作：
相关代码目录：
相关文档：
可在答辩中说明的内容：
```

示例：

```text
姓名：成员 C
负责模块：payment-service、RabbitMQ、支付幂等
完成工作：实现支付确认接口、金额校验、订单状态校验、支付流水落库、Redis 幂等控制、支付成功事件发布。
相关代码目录：backend/payment-service
相关文档：docs/api.md、docs/payment-design.md
可在答辩中说明的内容：支付前校验流程、重复支付拦截方式、RabbitMQ 事件驱动设计。
```

## 13. 总结

本分工方案保证 5 名成员都围绕 MiniPay 的核心系统能力承担明确责任。成员 A 负责整体架构和集成，成员 B 负责订单与商户，成员 C 负责支付与消息，成员 D 负责认证、通知和运营后台，成员 E 负责前端、部署和质量治理。

这种分工能够覆盖期末工程要求的主链路、服务协同、容器化、CI、监控、链路追踪和文档交付，也便于答辩时每位成员清楚说明自己的设计与实现。
